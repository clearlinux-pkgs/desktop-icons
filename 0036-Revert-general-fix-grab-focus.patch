From 924d1147c610798e14e27f13e434299233d5b7ae Mon Sep 17 00:00:00 2001
From: Carlos Soriano <csoriano1618@gmail.com>
Date: Sun, 9 Dec 2018 18:21:27 +0000
Subject: [PATCH 36/55] Revert "general: fix grab focus"

This reverts commit fc79021e67f8ec01d212a6583e71209b77d03a9a
---
 desktopGrid.js    | 4 ----
 desktopManager.js | 3 ---
 2 files changed, 7 deletions(-)

diff --git a/desktopGrid.js b/desktopGrid.js
index 239de12..ffc42fc 100644
--- a/desktopGrid.js
+++ b/desktopGrid.js
@@ -118,10 +118,6 @@ var DesktopGrid = class {
         this.reset();
     }
 
-    grabFocus() {
-        this._grid.grab_key_focus();
-    }
-
     _onKeyPress(actor, event) {
         if (global.stage.get_key_focus() != actor)
             return Clutter.EVENT_PROPAGATE;
diff --git a/desktopManager.js b/desktopManager.js
index c05895d..ebfb8d7 100644
--- a/desktopManager.js
+++ b/desktopManager.js
@@ -94,9 +94,6 @@ var DesktopManager = class {
         Extension.lockActivitiesButton = true;
         this._stageReleaseEventId = global.stage.connect('button-release-event', (actor, event) => {
             this.endRubberBand();
-            /* Since none of the keyboard operations are managed in the grid itself, but are simply
-             * redirected to desktopManager, it is fine to always give the focus to the first grid */
-            this._desktopGrids[0].grabFocus();
         });
         this._rubberBandId = global.stage.connect('motion-event', (actor, event) => {
             [x, y] = event.get_coords();
-- 
2.20.0

